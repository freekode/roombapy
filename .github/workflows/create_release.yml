on: push
    
name: Create Release

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
            fetch-depth: 0
      - name: Retrieve version
        run: |
            echo ::set-env name=GIT_RELEASE_NOTES::`git log $(git describe --tags --abbrev=0)..HEAD --pretty=format:'%h %s'`
            echo ${{ env.GIT_RELEASE_NOTES }}
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            Commits
            ${{ env.GIT_RELEASE_NOTES }}
            
            Test
            
            ${{ 
          draft: true
          prerelease: false
